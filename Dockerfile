# Используем официальный PHP образ с встроенным веб-сервером
FROM php:8.2-cli

# Копируем все файлы проекта в контейнер
WORKDIR /app
COPY . /app

# Открываем порт 10000
EXPOSE 10000

# Запускаем встроенный PHP-сервер
CMD ["php", "-S", "0.0.0.0:10000", "bot.php"]
